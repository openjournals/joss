<!DOCTYPE html>
<html lang="en">
<title>The Journal of Open Source Software</title>
<%= stylesheet_link_tag    'application', media: 'all' %>
<%= javascript_include_tag 'application' %>
<meta name="viewport" content="width=device-width, initial-scale=1">
<%= csrf_meta_tags %>
<body>
  <header class="masthead">
    <div class="container">
      <a href="/" class="masthead-logo">
      <span class="mega-octicon octicon-book"></span>
        The Journal of Open Source Software
      </a>

      <nav class="masthead-nav">
        <%= link_to "Submit", new_paper_path %>
        <%= link_to "Papers", papers_path %>
        <%= link_to "About", about_path %>
        <% if current_user %>
          <%= link_to current_user.nice_name, profile_path, :class => "profile-link" %> &middot; <%= link_to "Sign out", signout_path %>
        <% else %>
          <%= link_to "Sign in", "/auth/orcid", :class => "btn btn-reverse" %>
        <% end %>
      </nav>
    </div>
  </header>
  <div class="container">
    <%= render :partial => "shared/flashes" %>
    <%= render :partial => "shared/profile" if show_profile_banner? %>
    <%= yield %>
  </div>
</body>
</html>
