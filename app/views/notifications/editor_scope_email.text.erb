INSTRUCTIONS

Please review the recently-flagged submissions below and vote on the <%= Rails.application.settings["abbreviation"] %> paper page whether you believe the submission is in scope, or out of scope.

<% if @query_scope_issues.any? %>
  <% @query_scope_issues.each do |issue| %>
    <% unless Vote.has_vote_for?(issue.paper, @editor) %>
      <% if issue.pre_review? %>

<%= issue.title %> (<%= issue.paper.created_at.strftime('%F') %>)
  
<%= issue.paper.body %>
  
VOTE ON SCOPE HERE: <%= Rails.application.settings["url"] %>/papers/<%= issue.paper.sha %>
Review issue: https://github.com/<%= Rails.application.settings["reviews"] %>/issues/<%= issue.number %>
Software repository: <%= issue.paper.repository_url %>

–––––––––––––––––––––––––––––––––––––––––––––––––––––––––––––––––––------------
      <% end %>
    <% end %>
  <% end %>
<% end %>